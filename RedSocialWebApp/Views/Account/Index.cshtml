@{
    Layout = "_Layout"; 
    ViewData["Title"] = "Bienvenido a SctNet";
}

<div class="bg-image" style="background-image: url('/images/home-bg.jpg');">
    <div class="overlay d-flex justify-content-center align-items-center">
        <div class="container text-center">
            <h1 class="display-3 mb-4">¡Bienvenido a SctNet Tu Red Social!</h1>
            <p class="lead mb-5">Conéctate, comparte y descubre. Todo en un solo lugar.</p>
            <div class="d-flex justify-content-center flex-column flex-sm-row gap-3">
                <a asp-action="Login" class="btn btn-primary-custom btn-large">Iniciar sesión</a>
                <a asp-action="Register" class="btn btn-outline-custom btn-large">Crear cuenta</a>
            </div>
        </div>
    </div>
</div>
